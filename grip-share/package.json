{
  "name": "@glade/grip-share",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Grip Share declaration plumbing (post GC-3): the share-space vocabulary — ShareDecl share hooks, manifest-driven domain/zone -> wire (share, key) scope, payload-codec shape, glade-id manifest input. The binder (direct tap<->glade session coupling) was deleted; bindings are glial mounts.",
  "scripts": {
    "test": "node --experimental-transform-types --test test/*.test.ts"
  },
  "devDependencies": {
    "@owebeeone/glade-decl": "link:../../glade-decl-ts",
    "@owebeeone/glial-runtime": "link:../../glial"
  }
}
